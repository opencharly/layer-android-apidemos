# 2026.250.1816 — fix: ship the committed ApiDemos APK fixture the manifest declares

## Summary

The candy manifest declares `apk: tests/data/ApiDemos-debug.apk` (the committed-file path of the `apk:` package format), but the de-submodule cutover moved the candy out of charly without its fixture — the repo never contained `tests/data`. The path contract is project-root-relative to the CANDY's own source tree, so the standalone repo must carry the APK itself. This commits the canonical charly test fixture (md5 `f968ec5b` — the same file charly ships at `tests/data/ApiDemos-debug.apk`) into the repo. The `device-net` member of check-android-emulator-pod then installs ApiDemos via goadb instead of failing preresolve.

## How tested

**E-4 R10 bed run** (check-android-emulator-pod, `disposable: true`, this head composed via `CHARLY_REPO_OVERRIDE`), calver `2026.250.1755`:

```
[bring-up-members] PASS after 41.4s
  PASS  check adb=shell  [apk-net-apidemos-present]  package:io.appium.android.apis   ← installed via goadb from this fixture
```

The pre-fix failure (calver `2026.250.1712`):

```
deploy "device-net": candy "android-apidemos": committed APK "tests/data/ApiDemos-debug.apk" not found under candy source tree "/home/atrawog/.cache/charly/repos/github.com/opencharly/layer-android-apidemos@v2026.239.1626"
```

md5 verification after commit: `f968ec5b199827ffd8607e51a8b50a32 tests/data/ApiDemos-debug.apk` (identical to charly's fixture).

## Rulebook compliance

- R1: root-caused to the de-submodule cutover dropping the fixture (the repo never had it; 5-commit history, no apk paths).
- R3: the fixture lives in the candy's own source tree, matching the committed-file path contract; the charly copy is the canonical source.
- R5: no legacy path involved.

## Change classification

- Change class: committed asset addition (cutover-residue fixture)
- Verification gate: E-4 R10 bed run (bring-up install of ApiDemos via goadb — analysed on a live system)
- Attribution tier: analysed on a live system

*Assisted-by: pi openrouter/auto (analysed on a live system)*